import 'package:flutter/material.dart';

class FactSelection extends StatelessWidget {
  static const routeName = '/facts';

  const FactSelection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
